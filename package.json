{
  "name": "gatsby-remote-debug",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:cache": "cd gatsby-site && gatsby clean && NETLIFY_BUILD_BASE=\"../cache\" gatsby build",
    "build:gatsby": "cd gatsby-site && npm i && npm run build",
    "deploy:server": "git subtree push --prefix image-server heroku master",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
